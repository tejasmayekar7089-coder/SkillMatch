# SkillMatch Frontend Environment Configuration (Vercel / Local)

# Production FastAPI Backend Base URL (e.g. on Render / Railway)
# For local development: leave empty to use Vite proxy (/api)
# For production: point to your deployed backend URL
VITE_API_BASE_URL=https://skillmatch-backend.onrender.com/api
